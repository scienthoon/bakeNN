# Independent reproduction results

JSON files in this directory are submitted by external contributors following
[`docs/CLEAN_ROOM_REPRODUCTION.md`](../docs/CLEAN_ROOM_REPRODUCTION.md).

The maintainer does not pre-populate a passing result here: doing so would not
be independent evidence. A result records artifact hashes and generated-C
output agreement, not MCU performance.

